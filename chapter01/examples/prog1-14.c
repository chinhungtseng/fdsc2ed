/* Program 1.14 Simplified versino of Program 1.13 

   Examing Program 1.14, we can see that if count's initial value is 0,
   its final value will be 2n + 3. 
   Thus, each invocation of sum executes a total of 2n + 3 steps. */

float sum(float list[], int n)
{
    float tempsum = 0;
    int i;
    for (i = 0; i < n; i++)
        count += 2;
    count += 3;
    return 0;
}