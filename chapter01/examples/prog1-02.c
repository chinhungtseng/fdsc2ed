/* Program 1.2 Selection sort algorithm
   Fist attempt at deriving a solution. */

for (i = 0; i < n; i++) {
    Examine list[i] to list[n-1] and suppose that the
    smallest integer is at list[min];

    Interchange list[i] and list[min];
}
