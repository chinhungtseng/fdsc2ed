/* Exercise 5

   The pigeon hole principle states that if a function f has n distinct inputs but less than n distinct 
   outputs then there are two inputs a and b such that a != b and f(a) = f(b).
   Write a C program to find the values a and b for which the range values are equal. */

